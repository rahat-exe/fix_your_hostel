import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color bgDark = Color(0xFF0D111A);
  static const Color bg = Color(0xFF141A26);
  static const Color bgLight = Color(0xFF1C2333);

  // Text
  static const Color text = Color(0xFFF2F4FA);
  static const Color textMuted = Color(0xFFB8C0D9);

  // UI elements
  static const Color highlight = Color(0xFF5F6FB3);
  static const Color border = Color(0xFF4A567F);
  static const Color borderMuted = Color(0xFF343C59);

  // Brand / status
  static const Color primary = Color.fromARGB(255, 156, 170, 245);
  static const Color secondary = Color(0xFFFFC266);

  static const Color danger = Color(0xFFE57373);
  static const Color warning = Color(0xFFE6B566);
  static const Color success = Color(0xFF6ED6A3);
  static const Color info = Color(0xFF6FA8FF);
}
